# pip install -r requirements.txt

# 核心
requests>=2.28.0    # Ollama HTTP
mcp>=1.0.0          # MCP server（mcp_server.py 用，CLI 不需要）
numpy>=1.21.0       # RAG / MMR 向量運算

# 可選：缺了會由各功能 graceful skip，doctor 會提示
# pip install pymupdf4llm ollama   # RAG.py 從 PDF/MD 建知識庫
# pip install html2text            # RAG.py --url 抓網頁
# pip install jieba                # 中文 BM25 切詞
